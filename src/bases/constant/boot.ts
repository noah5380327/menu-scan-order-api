export const DEFAULT_CORS = false;

export const DEFAULT_PORT = 3000;

export const DEFAULT_API_PREFIX = '/api';

export const DEFAULT_LOG_LEVEL = 'info';

export const DEFAULT_LOG_FILE_DIRECTORY = 'log';

export const DEFAULT_LOG_FILE_PREFIX = 'dovejs';

export const DEFAULT_LOG_FILE_PATTERN = 'YYYY-MM-DD';

export const DEFAULT_LOG_FILE_MAX_SIZE = '20m';

export const DEFAULT_LOG_FILE_LABEL = 'NestWinston';

export const DEFAULT_DOC_DISABLED = false;

export const DEFAULT_DOC_TITLE = 'Swagger Document';

export const DEFAULT_DOC_DESC = 'The API description';

export const DEFAULT_DOC_VERSION = '1.0';

export const DEFAULT_BODY_PARSER_LIMIT = '50mb';

export const DEFAULT_ENABLE_IMPLICIT_CONVERSION = false;
